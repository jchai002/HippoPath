<nav id="menu-bar" class="navbar navbar-default navbar-static-top mar-b-0">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#">HippoPath<i class="fa fa-stethoscope"></i></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <%if current_user%>
        <li>
          <%=link_to interviews_dash_board_path do%>
          <i class="fa fa-list"></i>
          Post Interviews
          <%end%>
        </li>
        <li>
          <%=link_to search_dash_board_path do%>
          <i class="fa fa-search"></i>
          <span>Search Interviews</span>
          <%end%>
        </li>
        <li>
          <%=link_to '#', id: 'conversation-container-toggle', data: {'open-state' => '', 'state' => ''} do %>
          <i class="fa fa-wechat"></i>
          <span>Messages</span>
          <%end%>
        </li>
        <li class="conversation-list-anchor">
          <div class="conversation-container"></div>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">
            <%=image_tag current_user.image.exists? ? current_user.image.url(:small) : 'portraitplaceholder.png', class: 'menu-avatar'%>
            <%=current_user.name ? current_user.name.split(' ')[0] : current_user.email.split('@')[0]%> <span class="caret"></span>
          </a>
          <ul class="dropdown-menu">
            <li><%=link_to "Account Info", edit_user_path(current_user) %></li>
            <li role="separator" class="divider"></li>
            <li><%=link_to("logout", destroy_user_session_path, :method => :delete) %></li>
          </ul>
        </li>
        <%else%>
        <li><a href="/users/sign_in">login</a></li>
        <%end%>
      </ul>

    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
